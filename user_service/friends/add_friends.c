void add_friends(char username[]){
    // get who to add from user (scan)

    // check if user add self

    // check if user add someone not in database

    // if user account exist then
        // if first time then --> add it to database.txt
        // if second time then --> output fail (check samplemd) 
        // if already in incoming --> output fail (check samplemd)
}
